---
- hosts: webservers
  user: ansible
  become: true
  connection: ssh
  tasks:

     - name: Clone Flipkart Repository 
       git:
        repo: https://github.com/Msocial123/Flipkart-Clone.git
        dest: index.html
        version: main
        force: yes 